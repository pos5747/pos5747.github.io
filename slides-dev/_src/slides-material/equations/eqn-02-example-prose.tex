% Example prose asset (theorem cards, exam questions — anything with
% wrapping text). The minipage sets the measure; adjust its width to taste.
\documentclass[border=3bp]{standalone}
\input{eqn-preamble.tex}
\begin{document}

\begin{minipage}{3.5in}
\textbf{Invariance.} If $\hat{\theta}$ is the maximum likelihood estimate of
$\theta$, then for any function $\tau(\theta)$, the maximum likelihood
estimate of $\tau(\theta)$ is $\tau(\hat{\theta})$.
\end{minipage}

\end{document}
