% practice problem: scalar vs. matrix notation for regression (slide "time permitting")
% source recovered from wk01 deck presenter notes, 2026-07-22
\documentclass[border=3bp]{standalone}
\input{eqn-preamble.tex}
\begin{document}

\begin{minipage}{3.5in}
Show that scalar notation
$$
y_i = \beta_0 + \beta_1 x_{i1} + \dots + \beta_k x_{ik} + \epsilon_i
$$

is equivalent to the matrix notation

$$
y = X\beta + \epsilon.
$$
\end{minipage}

\end{document}
