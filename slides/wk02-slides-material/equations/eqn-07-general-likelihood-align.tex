% ML in general: the likelihood for any pmf/pdf, product form
% — source recovered from wk02 deck notes, 2026-07-22
\documentclass[border=3bp,varwidth=6.5in]{standalone}
\input{eqn-preamble.tex}
\begin{document}

% the deck shows this on the black ASIDE slide in its green overlay color
\definecolor{mygreen}{HTML}{00F900}
\color{mygreen}
\begin{align}
\text{the likelihood: } L(\pi)
  &= \prod_{i=1}^N f(y_i) \nonumber\\
  &= \prod_{i=1}^N \pi^{y_i} (1 - \pi)^{1 - y_i}\nonumber\\
  &= \text{\textit{(could simplify as before if we wanted...)}} \nonumber
\end{align}

\end{document}
