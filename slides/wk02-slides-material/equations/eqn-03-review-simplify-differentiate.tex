% review problem: simplify the Bernoulli log-likelihood and differentiate
% — source recovered from wk02 deck notes, 2026-07-22
\documentclass[border=3bp]{standalone}
\input{eqn-preamble.tex}
\begin{document}

\begin{minipage}{3.5in}
Let $\ell(\pi) = \log\bigl[\pi^k (1 - \pi)^{N - k}\bigr]$ for $0 < \pi < 1$ and $0 < k < N$. Simplify the right-hand side and find $\dfrac{d \ell(\pi)}{d\pi}$.
\end{minipage}

\end{document}
